var Debuggable = new Class({
	
	debug: function(object)
	{
	  debugModeON = (document.debugMode == true);
	  debugThisType = ( $defined(document.debugTypes) && (document.debugTypes[this.typeName] == true))
	  
		if (debugModeON || debugThisType)
		{
			console.log (object);
		}
	}	
});
